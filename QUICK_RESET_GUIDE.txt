========================================
QUICK STORAGE RESET GUIDE
========================================

✅ ALREADY SETUP - No configuration needed!

========================================
HOW TO USE (3 Easy Steps)
========================================

STEP 1: Open Console
--------------------
Option A: React Native Debugger
  - Open React Native Debugger app
  - Or use Chrome DevTools (if connected)

Option B: Metro Console
  - Look at your terminal where Expo is running
  - You can type commands there

Option C: In-App Console
  - Shake device → Dev Menu → "Debug Remote JS"
  - Opens browser console


STEP 2: Type Command
--------------------
Just type one of these:

  resetStorage()          ← Full reset (recommended)
  resetOnboarding()       ← Only reset onboarding
  showStorage()           ← Check what's stored


STEP 3: Reload App
------------------
- Shake device → "Reload"
- Or press 'r' in Metro terminal
- Or Cmd+R (iOS) / Cmd+M then Reload (Android)


========================================
EXAMPLE WORKFLOW
========================================

1. Open React Native Debugger
2. Type: resetStorage()
3. See: "✅ [DEV] Removed X storage key(s)"
4. Shake device → Reload
5. ✅ Onboarding screens appear!


========================================
AVAILABLE COMMANDS
========================================

resetStorage()
  → Clears ALL storage (onboarding + auth)
  → Use for complete reset

resetOnboarding()
  → Only clears onboarding status
  → Keeps your login session
  → Use to test onboarding without re-logging in

showStorage()
  → Shows all current storage keys
  → Use to debug what's stored


========================================
TIPS
========================================

✅ Commands work immediately (no UI needed)
✅ Automatically disabled in production
✅ Safe to use - only clears app storage
✅ Works every time you reload

========================================
TROUBLESHOOTING
========================================

Commands not showing?
  → Check Metro console for "🛠️ [DEV] Dev tools loaded!"
  → Make sure you're in development mode
  → Try reloading the app

Storage not resetting?
  → Make sure you reload after running command
  → Check with showStorage() to verify
  → Try uninstalling/reinstalling app

========================================

